{
	"name": "@conforall/workspace",
	"private": true,
	"type": "module",
	"version": "0.0.1",
	"scripts": {
		"prepare": "husky install",
		"format": "prettier -u --write ."
	},
	"workspaces": [
		"constants",
		"models",
		"preprocessor",
		"web"
	],
	"lint-staged": {
		"*": "prettier -u --write"
	},
	"devDependencies": {
		"@commitlint/cli": "^17.7.0",
		"@commitlint/config-conventional": "^17.7.0",
		"husky": "^8.0.3",
		"lint-staged": "^13.2.3",
		"prettier": "^3.0.1",
		"prettier-plugin-astro": "^0.11.0"
	}
}
